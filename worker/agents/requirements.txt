# LangGraph ReAct Executor Dependencies
# Install with: pip install -r requirements.txt
# Or using uv: uv pip install -r requirements.txt

# LangChain ecosystem
langchain>=0.3.0
langchain-core>=0.3.0
langchain-ollama>=0.2.0

# LangGraph for ReAct pattern
langgraph>=0.2.0

# Ollama client (used by langchain-ollama)
ollama>=0.3.0

# Type stubs for better IDE support (optional)
# types-requests>=2.31.0
